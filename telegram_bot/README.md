# 🤖 ربات تلگرام با API سایت chat01.ai

ربات تلگرام هوشمند با قابلیت چت با هوش مصنوعی از طریق API سایت chat01.ai

## ✨ ویژگی‌ها

- 💬 چت با هوش مصنوعی از طریق API سایت chat01.ai
- 🔄 قابلیت ایجاد و جابجایی بین گفتگوهای مختلف
- 🗑️ پاک کردن تاریخچه گفتگو
- 📊 نمایش آمار مصرف کاربر
- 👥 پنل مدیریت ادمین با امکانات:
  - بلاک/آن‌بلاک کاربران
  - تنظیم محدودیت پیام روزانه برای هر کاربر
  - نامحدود کردن کاربران
  - مشاهده لیست کاربران
- 🎹 رابط کاربری با دکمه‌های اینلاین
- 💾 ذخیره‌سازی داده‌ها در دیتابیس SQLite

## 🚀 نصب سریع (خودکار)

فقط دستور زیر را اجرا کنید:

```bash
bash install.sh
```

اسکریپت به صورت خودکار:
1. پیش‌نیازها را بررسی و نصب می‌کند
2. اطلاعات مورد نیاز را می‌پرسد
3. فایل‌ها را ایجاد می‌کند
4. ربات را اجرا می‌کند

## 📋 پیش‌نیازها

- Python 3.8+
- pip
- توکن ربات تلگرام از [@BotFather](https://t.me/BotFather)
- API Key سایت [chat01.ai](https://chat01.ai)

## ⚙️ نصب دستی

### 1. کلون کردن یا دانلود فایل‌ها

### 2. ایجاد محیط مجازی
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. نصب وابستگی‌ها
```bash
pip install -r requirements.txt
```

### 4. تنظیم config.py
فایل `config.py` را ویرایش کنید و مقادیر زیر را وارد کنید:
- `BOT_TOKEN`: توکن ربات تلگرام
- `CHAT01_API_KEY`: API Key سایت chat01.ai
- `ADMIN_ID`: آیدی عددی تلگرام ادمین
- `DEFAULT_MODEL`: مدل پیش‌فرض (مثلاً `gpt-4o`)

### 5. اجرای ربات
```bash
python3 bot.py
```

## 📝 دستورات

### دستورات عمومی
| دستور | توضیحات |
|--------|---------|
| `/start` | شروع ربات و نمایش منوی اصلی |

### دستورات ادمین
| دستور | توضیحات |
|--------|---------|
| `/admin` | نمایش پنل مدیریت |
| `/block [user_id]` | بلاک کردن کاربر |
| `/unblock [user_id]` | آن‌بلاک کردن کاربر |
| `/setlimit [user_id] [limit]` | تنظیم محدودیت پیام روزانه |
| `/setlimit [user_id] -1` | نامحدود کردن کاربر |

## 🎹 دکمه‌های منوی اصلی

| دکمه | عملکرد |
|------|--------|
| ✨ گفتگوی جدید | شروع یک گفتگوی جدید |
| 📝 گفتگوهای من | نمایش لیست گفتگوها و امکان جابجایی |
| 🗑 پاک کردن تاریخچه | پاک کردن پیام‌های گفتگوی فعلی |
| 📊 آمار من | نمایش آمار مصرف روزانه |

## 🔧 ساختار فایل‌ها

```
telegram_bot/
├── bot.py              # فایل اصلی ربات
├── config.py           # تنظیمات
├── database.py         # مدیریت دیتابیس
├── requirements.txt    # وابستگی‌های پایتون
├── install.sh          # اسکریپت نصب خودکار
└── README.md           # راهنما
```

## 🔐 متغیرهای محیطی (اختیاری)

می‌توانید به جای ویرایش `config.py` از متغیرهای محیطی استفاده کنید:

```bash
export BOT_TOKEN="your_bot_token"
export CHAT01_API_KEY="your_api_key"
export ADMIN_ID="123456789"
export DEFAULT_MODEL="gpt-4o"
```

## 📊 مدل‌های موجود

| مدل | توضیحات |
|-----|---------|
| `gpt-4o` | مدل چندوجهی GPT-4o |
| `gpt-5-2` | GPT-5.2 |
| `gpt-5-2-thinking` | GPT-5.2 با قابلیت استدلال |
| `gpt-5-2-instant` | GPT-5.2 سریع |
| `gpt-5-1-thinking` | GPT-5.1 با قابلیت استدلال |
| `gpt-5-1-instant` | GPT-5.1 سریع |
| `o3` | مدل O3 |

## ⚠️ نکات مهم

1. **محدودیت پیش‌فرض**: هر کاربر به صورت پیش‌فرض 20 پیام در روز می‌تواند ارسال کند.
2. **ریست روزانه**: شمارنده پیام‌ها هر روز در ساعت 00:00 UTC ریست می‌شود.
3. **دیتابیس**: اطلاعات در فایل `bot_database.db` ذخیره می‌شود.

## 🐛 عیب‌یابی

### ربات پاسخ نمی‌دهد
- مطمئن شوید توکن ربات صحیح است
- لاگ‌ها را بررسی کنید

### خطای API
- مطمئن شوید API Key سایت chat01.ai معتبر است
- اعتبار حساب خود را بررسی کنید

### خطای اتصال
- اتصال اینترنت را بررسی کنید
- مطمئن شوید سایت chat01.ai در دسترس است

## 📄 مجوز

MIT License

## 👨‍💻 توسعه‌دهنده

ساخته شده با ❤️
